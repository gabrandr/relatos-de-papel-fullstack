services:
  eureka-server:
    build:
      context: ./relatos-de-papel-backend/eureka-server
    container_name: eureka-server
    restart: unless-stopped
    environment:
      PORT: 8761
      HOSTNAME: eureka-server
    ports:
      - "8761:8761"

  postgres-payments:
    image: postgres:17-alpine
    container_name: postgres-payments
    restart: unless-stopped
    environment:
      POSTGRES_DB: payments_db
      POSTGRES_USER: payments_user
      POSTGRES_PASSWORD: payments_pass
    ports:
      - "5432:5432"
    volumes:
      - postgres_payments_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U payments_user -d payments_db"]
      interval: 10s
      timeout: 5s
      retries: 10

  ms-books-catalogue:
    build:
      context: ./relatos-de-papel-backend/ms-books-catalogue
    container_name: ms-books-catalogue
    restart: unless-stopped
    depends_on:
      - eureka-server
    env_file:
      - ./relatos-de-papel-backend/.env
    environment:
      PORT: 8081
      HOSTNAME: ms-books-catalogue
      EUREKA_URL: http://eureka-server:8761/eureka
      EUREKA_ENABLED: true
      EUREKA_REGISTER: true
      EUREKA_FETCH: true
      OPENSEARCH_INDEX: ${OPENSEARCH_INDEX:-relatos}
    ports:
      - "8081:8081"

  ms-books-payments:
    build:
      context: ./relatos-de-papel-backend/ms-books-payments
    container_name: ms-books-payments
    restart: unless-stopped
    depends_on:
      eureka-server:
        condition: service_started
      postgres-payments:
        condition: service_healthy
    environment:
      PORT: 8082
      HOSTNAME: ms-books-payments
      EUREKA_URL: http://eureka-server:8761/eureka
      EUREKA_ENABLED: true
      EUREKA_REGISTER: true
      EUREKA_FETCH: true
      POSTGRES_HOST: postgres-payments
      POSTGRES_PORT: 5432
      POSTGRES_DB: payments_db
      POSTGRES_USER: payments_user
      POSTGRES_PASSWORD: payments_pass
      SQL_INIT_MODE: never
      JPA_DDL_AUTO: update
    ports:
      - "8082:8082"

  gateway:
    build:
      context: ./relatos-de-papel-backend/gateway
    container_name: gateway
    restart: unless-stopped
    depends_on:
      - eureka-server
      - ms-books-catalogue
      - ms-books-payments
    environment:
      PORT: 8762
      HOSTNAME: gateway
      EUREKA_URL: http://eureka-server:8761/eureka
    ports:
      - "8762:8762"

  frontend:
    build:
      context: ./relatos-de-papel-frontend
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://localhost:8762}
    container_name: relatos-frontend
    restart: unless-stopped
    depends_on:
      - gateway
    ports:
      - "5173:80"

volumes:
  postgres_payments_data:
